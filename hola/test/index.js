const lib = require('./../index');

const expect = require('chai').expect;

describe('hola\test tests', () => {
    
    it('Suma correcta', () => {
        
        expect(1 + 1).to.equal(2);
    });
});